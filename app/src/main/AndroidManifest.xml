<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mardiak.marek.hrapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!--<uses-feature android:glEsVersion="0x00020000" android:required="true" />-->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".activities.EmployeeListActivity"
            android:label="@string/app_name" >
<!--            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:screenOrientation="landscape"
            android:name=".activities.openglDemo2.LessonOneActivity"
            android:label="@string/app_name" >
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:screenOrientation="landscape"
            android:name=".hellojni.HelloJni"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:exported="true"
            android:readPermission="true" android:writePermission="true"
            android:name="OrgStructureContentProvider"
            android:authorities="com.example.mardiak.marek.hrapp.contentprovider"
            />
    </application>

</manifest>
